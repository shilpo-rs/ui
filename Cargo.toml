[workspace]
members = [
    "macros",
    "theme",
    "m3e",
    "storybook",
]
resolver = "2"

[workspace.package]
publish = false
edition = "2024"

[workspace.dependencies]
shilpo-macros = { path = "macros" }
shilpo-theme = { path = "theme" }
shilpo-m3e = { path = "m3e" }
criterion = { version = "*", package = "codspeed-criterion-compat" }

gpui = { git = "https://github.com/zed-industries/zed" }
gpui_platform = { git = "https://github.com/zed-industries/zed", features = ["font-kit", "x11", "wayland", "runtime_shaders"] }
gpui_macros = { git = "https://github.com/zed-industries/zed" }
reqwest = { git = "https://github.com/zed-industries/reqwest.git", rev = "c15662463bda39148ba154100dd44d3fba5873a4", default-features = false, features = [
    "blocking",
    "charset",
    "http2",
    "macos-system-configuration",
    "multipart",
    "rustls-tls-native-roots",
    "socks",
    "stream",
], package = "zed-reqwest", version = "0.12.15-zed" }
reqwest_client = { git = "https://github.com/zed-industries/zed" }
gpui_web = { git = "https://github.com/zed-industries/zed" }
sum-tree = { version = "0.2", package = "zed-sum-tree" }

anyhow = "1"
dirs = "6"
log = "0.4"
lsp-types = { version = "0.97", features = ["proposed"] }
notify = "8"
notify-rust = "4"
raw-window-handle = "0.6"
ropey = { version = "2.0.0-beta.1", features = ["metric_lines_lf", "metric_utf16"] }
rust-i18n = "4"
schemars = "1"
serde = { version = "1", features = ["derive", "rc"] }
serde_json = "1"
serde_repr = "0.1"
smallvec = "1"
smol = "2"
tracing = "0.1"
mcu-material-color = { version = "0.2", default-features = false }
proptest = "1"

[workspace.dependencies.windows]
features = ["Wdk", "Wdk_System", "Wdk_System_SystemServices"]
version = "0.62"

[workspace.lints.clippy]
almost_complete_range = "allow"
arc_with_non_send_sync = "allow"
borrowed_box = "allow"
dbg_macro = "deny"
let_underscore_future = "allow"
manual_is_multiple_of = "allow"
map_entry = "allow"
module_inception = "allow"
non_canonical_partial_ord_impl = "allow"
reversed_empty_ranges = "allow"
single_range_in_vec_init = "allow"
style = { level = "allow", priority = -1 }
todo = "deny"
type_complexity = "allow"
useless_conversion = "allow"

[profile.dev]
codegen-units = 16
debug = "limited"
split-debuginfo = "unpacked"

[profile.dev.package]
resvg = { opt-level = 3 }
rustybuzz = { opt-level = 3 }
taffy = { opt-level = 3 }
ttf-parser = { opt-level = 3 }
smol = { opt-level = 3 }
gpui = { opt-level = 3 }
gpui_platform = { opt-level = 3 }
gpui_macros = { opt-level = 3 }
tree-sitter = { opt-level = 3 }
sum_tree = { opt-level = 3 }
ropey = { opt-level = 3 }
lsp-types = { opt-level = 3 }
reqwest_client = { opt-level = 3 }
markdown = { opt-level = 3 }
xml5ever = { opt-level = 3 }

[workspace.metadata.typos]
files.extend-exclude = ["**/fixtures/*"]

[workspace.metadata.typos.default.extend-identifiers]
consts = "consts"

[workspace.metadata.cargo-machete]
ignored = ["log", "anyhow", "serde"]

# Patches for WASM compatibility
[patch.crates-io]
# Use stacker's psm version which may have better WASM support
psm = { git = "https://github.com/rust-lang/stacker", branch = "master" }

# Fork of gpui adding ext-session-lock-v1 support, used by shilpo-rs/shilpo. Pinned by exact
# rev, never by branch, so this build is reproducible regardless of later fork commits, and so
# every crate in the org building against gpui uses the exact same fork revision.
[patch."https://github.com/zed-industries/zed"]
gpui = { git = "https://github.com/sayeed205/zed", rev = "9e336ac", version = "=0.2.2" }
gpui_platform = { git = "https://github.com/sayeed205/zed", rev = "9e336ac", version = "=0.1.0" }
gpui_macros = { git = "https://github.com/sayeed205/zed", rev = "9e336ac", version = "=0.1.0" }
gpui_web = { git = "https://github.com/sayeed205/zed", rev = "9e336ac", version = "=0.1.0" }
reqwest_client = { git = "https://github.com/sayeed205/zed", rev = "9e336ac", version = "=0.1.0" }
