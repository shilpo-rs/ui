# Changelog

UI release notes are generated from merged pull-request titles with `git-cliff`. See [`RELEASES.md`](RELEASES.md).
Historical migrated tags are intentionally not treated as UI releases.

